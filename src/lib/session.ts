/**
 * Mock session. No auth exists yet — swap for the real signed-in
 * employee id once auth lands. Mirrors the admin side, which also
 * hardcodes its current user (see Navbar.tsx).
 */
export const CURRENT_EMPLOYEE_ID = "emp-001";
